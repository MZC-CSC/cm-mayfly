# gitleaks baseline — 수용된 오탐(false positive). 형식: <file>:<rule>:<line>
# 신규 실제 시크릿은 계속 차단되고, 아래 항목만 예외 처리된다.
# 검토일 2026-06-26 (보안 게이트 도입).

# 소스 주석/문서의 예시 curl 명령(localhost·예시 자격증명) — 실제 시크릿 아님
cmd/setup/credential.go:curl-auth-user:9
cmd/setup/credential.go:curl-auth-user:15
cmd/setup/credential.go:curl-auth-user:299
docs/upgrade-cb-tb-0.12.9.md:curl-auth-user:120
docs/upgrade-cb-tb-0.12.9.md:curl-auth-user:124

# vendored cb-tumblebug 기본 설정의 샘플 JWT(업스트림 기본값, 우리 시크릿 아님)
conf/docker/conf/cb-tumblebug/conf/grpc_conf.yaml:jwt:26

# 마스킹 동작을 검증하는 단위 테스트의 더미 값 — 실제 크리덴셜이 아니다.
# MaskSecret 이 앞 두 글자만 남기는지 확인하려면 토큰 형태의 입력이 필요하다.
common/mask_test.go:generic-api-key:16
common/mask_test.go:vault-service-token:35

# 하드코딩된 default:default 가 더 이상 전송되지 않는지 확인하는 회귀 테스트.
# 이 문자열이 응답에 없어야 통과하므로, 값 자체가 검증 대상이다.
cmd/setup/credential_test.go:generic-api-key:114
